#lang info

(define collection "json-comb")

(define deps '("base"
               "typed-racket-lib"
               "typed-racket-more"
               "json-type-provider"))

(define pkg-desc "Well-typed parser for JSON inspired by Type Provider")

(define pkg-authors '(pcn))
